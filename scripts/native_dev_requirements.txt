# Dev-only deps for running worker/masker.py directly, bypassing Docker.
# Useful on Windows where Docker Desktop's VM layer adds ~10x overhead on
# CPU-heavy precision inference. For deployment, use docker compose instead.
#
# Install via scripts/native_dev.sh, or manually:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.4.1 torchvision==0.19.1
#   pip install -r scripts/native_dev_requirements.txt
mediapipe==0.10.14
opencv-python==4.8.1.78
numpy==1.26.4
transformers==4.57.6
timm==1.0.26
pillow
